SELECT
	CITY, MAX(LENGTH(CITY))
FROM
	STATION
GROUP BY
	CITY
HAVING
	LENGTH(CITY) = MAX(LENGTH(CITY));
ORDER BY
	CITY

SELECT
	CITY, MIN(LENGTH(CITY))
FROM
	STATION
GROUP BY
	CITY
HAVING
	LENGTH(CITY) = MIN(LENGTH(CITY));
ORDER BY
	CITY
